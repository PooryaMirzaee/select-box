/** لینک‌های ناوبری هدر — از API کاتالوگ */

export type HeaderNavLink = {
  id: number;
  label_fa: string;
  href: string;
  sort_order: number;
  is_active: boolean;
  open_in_new_tab: boolean;
};
