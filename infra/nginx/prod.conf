# =============================================================================
# Nginx — Reverse Proxy تولید (یک دامنه برای فرانت + API)
# -----------------------------------------------------------------------------
# مسیرها:
#   /           → Next.js (web:3000)
#   /api/       → FastAPI (api:8000)
#   /api/media/ → بازنویسی به /api/v1/media/ روی API
#   /torob_api/ → FastAPI (فید ترب)
#   /health     → سلامت API
# =============================================================================

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

upstream web_upstream {
    server web:3000;
}

upstream api_upstream {
    server api:8000;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 64m;

    proxy_http_version 1.1;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;

    # FastAPI — Host داخلی تا TrustedHostMiddleware با IP/پورت مرورگر خطا ندهد
    location /api/media/ {
        rewrite ^/api/media/(.*)$ /api/v1/media/$1 break;
        proxy_set_header Host api;
        proxy_pass http://api_upstream;
    }

    location /api/ {
        proxy_set_header Host api;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_pass http://api_upstream;
    }

    location /torob_api/ {
        proxy_set_header Host api;
        proxy_pass http://api_upstream;
    }

    location /health {
        proxy_set_header Host api;
        proxy_pass http://api_upstream;
    }

    location / {
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_pass http://web_upstream;
    }
}
